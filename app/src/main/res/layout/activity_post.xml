<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:context=".ActivityPost">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <include
            android:id="@+id/layout_post_content_view"
            layout="@layout/layout_post_content_view"
            android:isScrollContainer="true" />

        <include
            android:id="@+id/layout_tags_content_view"
            layout="@layout/layout_tags_content_view"
            android:isScrollContainer="true" />

        <include
            android:id="@+id/layout_tags_content_view"
            layout="@layout/layout_comment"
            android:isScrollContainer="true" />

    </LinearLayout>

</ScrollView>